apiVersion: apps/v1
kind: Deployment
metadata:
  name: community-service
spec:
  template:
    spec:
      containers:
        - name: community-service
          envFrom:
            - secretRef:
                name: community-db-secret
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: prod
