apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/user-service
  - ../../../shared/database

patches:
  - path: patch-image.yaml
  - path: endpoint-dev.yaml
  - path: user-db-secret.yaml

namePrefix: dev-
