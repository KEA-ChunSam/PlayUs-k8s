apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: yukyung0/playus-user-service:latest
          ports:
            - containerPort: 8080
              name: http
          resources:
            requests: # 최소 보장 리소스
              cpu: "200m"
              memory: "256Mi"
            limits: # 최대 허용 리소스
              cpu: "400m"
              memory: "512Mi"
