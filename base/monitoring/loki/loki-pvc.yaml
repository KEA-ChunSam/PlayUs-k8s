# dev/prod 스토리지 용량/클래스가 다를 경우 overlay patch 고려
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loki-storage-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
