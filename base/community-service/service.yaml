apiVersion: v1
kind: Service
metadata:
  name: community-service
  namespace: community-service
  labels:
    app: community-service
spec:
  selector:
    app: community-service
  ports:
    - port: 80
      targetPort: 8080
      name: http
  type: ClusterIP
