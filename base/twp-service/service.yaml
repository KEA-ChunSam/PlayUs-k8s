apiVersion: v1
kind: Service
metadata:
  name: twp-service
  namespace: twp-service
  labels:
    app: twp-service
spec:
  selector:
    app: twp-service
  ports:
    - port: 80
      targetPort: 8080
      name: http
  type: ClusterIP
