apiVersion: v1
kind: ConfigMap
metadata:
  name: twp-service-config
  labels:
    app: twp-service
data:
  # JVM 설정
  JVM_OPTS: "-Xms512m -Xmx768m -XX:+UseContainerSupport -XX:+UseG1GC -XX:MaxGCPauseMillis=200"
