# Kong Ingress Chart 기본 설정
namespace: default
host: null
stripPath: true
pathType: Prefix

# 서비스 설정
service:
  name: default-service
  port: 80

# 경로 설정 (배열 형태)
paths:
  - /

# 커스텀 어노테이션
customAnnotations: {}
