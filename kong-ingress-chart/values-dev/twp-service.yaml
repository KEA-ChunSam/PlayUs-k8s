services:
  twp-service:
    namespace: dev-twp-service
    service:
      name: twp-service
      port: 80
    paths:
      - path: /twp
    pathType: Prefix
    stripPath: true

    # Kong 어노테이션 명시적 추가
    customAnnotations:
      konghq.com/preserve-host: "true"
