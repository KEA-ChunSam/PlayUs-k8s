namespace: dev-search-service

service:
  name: search-service
  port: 80

# 경로 설정 (rewrite는 Ingress level에서 처리)
paths:
  - path: /api/search

# Kong의 정규식 기반 rewrite를 사용하므로 ImplementationSpecific 권장
pathType: ImplementationSpecific
