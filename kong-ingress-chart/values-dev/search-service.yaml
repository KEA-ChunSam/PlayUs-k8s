services:
  search-service:
    namespace: dev-search-service
    service:
      name: search-service
      port: 80
    paths:
      - path: /search
    pathType: Prefix
    stripPath: true

    # Kong 어노테이션 명시적 추가
    customAnnotations:
      konghq.com/preserve-host: "true"
