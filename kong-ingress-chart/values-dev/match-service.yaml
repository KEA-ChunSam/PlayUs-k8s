namespace: dev-match-service

service:
  name: match-service
  port: 80

paths:
  - /api/match

targetPath: "/match"
pathType: Prefix
#host: api.example.com     # ip.nip.io

customAnnotations:
  konghq.com/rewrite: "/match"
