services:
  user-service:
    namespace: dev-user-service
    service:
      name: user-service
      port: 80
    paths:
      - path: /user
    pathType: Prefix
    stripPath: true

    # Kong 어노테이션 명시적 추가
    customAnnotations:
      konghq.com/preserve-host: "true"
