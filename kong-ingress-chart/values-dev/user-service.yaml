namespace: dev-user-service

service:
  name: user-service
  port: 80

# 경로 설정 (rewrite는 Ingress level에서 처리)
paths:
  - path: /api/user
  - path: /api/user/health

pathType: ImplementationSpecific

annotations:
  konghq.com/path-regex: "^/api/user/(.*)$"
  konghq.com/rewrite: "/$1"
  