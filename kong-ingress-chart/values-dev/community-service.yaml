services:
  community-service:
    namespace: dev-community-service
    service:
      name: community-service
      port: 80
    paths:
      - path: /community
    pathType: Prefix
    stripPath: true

    # Kong 어노테이션 명시적 추가
    customAnnotations:
      konghq.com/preserve-host: "true"
