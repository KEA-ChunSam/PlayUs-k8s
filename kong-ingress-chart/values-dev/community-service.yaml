namespace: dev-community-service

service:
  name: community-service
  port: 80

paths:
  - /api/post
  - /api/comment
  - /api/live-match-diary

stripPath: false
pathType: Prefix

customAnnotations:
  konghq.com/rewrite: "/$1" 